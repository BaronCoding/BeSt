import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SentItems = () => {
    return (
        <View>
            <Text>Sent items Screen</Text>
        </View>
    )
};

const styles = StyleSheet.create({});

export default SentItems;